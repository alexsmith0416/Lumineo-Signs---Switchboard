# Local dev cross-link: where Sign Builder Pro runs under `npm run dev`.
# Used to build the "Open original in Sign Builder Pro" link. Production
# deploys set VITE_SIGN_BUILDER_URL in the hosting project instead.
VITE_SIGN_BUILDER_URL=http://localhost:4801/
