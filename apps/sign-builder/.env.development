# Local dev cross-link: where Estimating runs under `npm run dev`.
# Used by the "Send to Estimating" button. Production deploys set
# VITE_ESTIMATING_URL in the hosting project instead.
VITE_ESTIMATING_URL=http://localhost:4802/
